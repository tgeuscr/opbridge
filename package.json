{
  "name": "heptad",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "overrides": {
    "@noble/hashes": "2.0.1"
  },
  "workspaces": [
    "apps/*",
    "contracts/ethereum",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @heptad/web",
    "build": "npm run build --workspaces --if-present && npm --prefix contracts/opnet run build",
    "test": "npm run test --workspaces --if-present && npm --prefix contracts/opnet run test",
    "typecheck": "npm run typecheck --workspaces --if-present && npm --prefix contracts/opnet run typecheck"
  }
}
